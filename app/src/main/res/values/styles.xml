<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ToolbarTheme" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <item name="colorControlNormal">@android:color/white</item>
    </style>

    <style name="LoadingTheme" parent="android:Theme.DeviceDefault.Dialog">
        <item name="DialogTitleAppearance">@android:style/TextAppearance.DeviceDefault.Medium</item>
        <item name="DialogTitleText">Tunggu Sebentar . . .</item>
        <item name="DialogSpotColor">@color/colorPrimaryDark</item>
    </style>

    <style name="FlatButton" parent="Theme.AppCompat.Light">
        <item name="android:buttonStyle">@style/Widget.AppCompat.Button.Borderless</item>
        <item name="colorControlHighlight">@color/colorPrimaryDarkLight</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

</resources>
